#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int* generiraj(int);
void shuffle(int*, int);
int* presjek(int*, int*, int, int);
int cmp(int*, int*);
int* presjek_sortiran(int*, int*, int, int);
int* presjek_quicksort(int*, int*, int, int);